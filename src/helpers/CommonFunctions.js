export const isEmpty = (value) => {
    const emptyArray = ['',null,undefined];
    if(emptyArray.includes(value)){
        return true;
    }
    return false;
}

const isKeyExistInObj = (key,obj) => {
    
}